# P2.3.3f — Adversaires + environnements

- Mappe l'attaque standard structurée des adversaires vers l'ActionField natif Foundryborne.
- Mappe `experience_text` vers `system.experiences` quand les entrées suivent `Nom +N`.
- Conserve les features adversaires/environnements en texte source : DH-DATA ne les expose pas encore comme enregistrements discrets fiables.
- Conserve les adversaires potentiels d'environnement en texte et marque explicitement le gap de résolution UUID vers le compendium `dh-adversaries`.
- Aucun contenu métier du template SRD n'est réintroduit.

Validation : réimport 1012/1012 puis `semanticAudit()`. Les gaps `adversary/system.attack` doivent fortement chuter et `adversary/system.experiences` être remplacés au pire par quelques `:parse`.
