# P2.10j.3h.1 — Hotfix syntaxe

Le remplacement automatique de fonctions de j.3h avait laissé un mot-clé
`async` seul sur une ligne avant `getNativeObjectActionContext()`.

Ce hotfix retire uniquement ce token orphelin.

Le ciblage DOM exact de j.3h est conservé.
