# P2.3.3h — Classes & subclasses SRD 2.0

Source: Daggerheart SRD 2.0, pp. 7–31.

## DH-DATA
- 13 classes SRD: `domains`, `starting_evasion`, `starting_hit_points`.
- 26 subclasses SRD: `spellcast_trait`; six martial subclasses encode `null` explicitly as `not_applicable`.

## Foundry mapping
- Maps class domains, starting Evasion and starting HP from source data.
- An explicit `null` Spellcast trait is treated as a valid non-spellcasting subclass, not a mapping gap.
- Semantic audit now checks class domain-pair/Evasion/HP completeness.

Expected effect on current audit: remove 13 `system.domains`, 13 `system.evasion`, and 26 `system.spellcastingTrait` gaps (52 total), while preserving explicit Blood Hunter feature gaps and other deferred mappings.
