# P2.3.3m — Isolated semantics closure

## Scope
Close the five isolated semantic gaps left after P2.3.3l without inventing automation.

### Spellcast weapons (4)
The Arcane-Frame wheelchair family uses the Daggerheart rules-level trait `Spellcast`.
Foundryborne's attack trait field accepts concrete actor abilities, so the importer keeps
`system.attack.roll.trait = null` and uses the ActionField default resolution when supported.
The source semantic is preserved in toolkit flags:

- `sourceAttackTrait: "spellcast"`
- `spellcastTraitResolution: "actor-default"`

This is treated as a resolved dynamic semantic, not a missing mapping.

### Temporal Enforcer direct damage (1)
The SRD standard attack is `40 direct`. The native Roll formula remains `40`; the `direct`
rider is preserved explicitly in toolkit provenance because it is a rules semantic, not Roll syntax:

- raw source string
- numeric formula
- `direct: true`
- meaning: damage cannot be reduced by marking Armor Slots

No automation is added in this phase.

## Audit
The semantic audit now verifies:

- dynamic Spellcast weapons retain the source semantic and a valid default-driven attack;
- direct-damage provenance agrees with the imported numeric attack formula.

Expected explicit gap count after a clean import: **338**.
