# P2.10h.5c — Rollback transactionnel

Cette version supprime les regex fragiles du patcher.

## Actor -> Backpack

Le patcher repère directement l'appel stable :

```js
const result = await api.expeditionItems.loadFromActor(manifest, {
```

Il ajoute le snapshot avant le `try` existant et le rollback dans son
`catch (error)`.

## Backpack -> Actor

Le patcher repère :

```js
async unloadToActor(
```

puis utilise un parseur d'accolades pour isoler la fonction complète.

## Garanties

Le patcher n'écrit les fichiers qu'après avoir construit avec succès les
deux transformations.
