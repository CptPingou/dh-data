# P2.3.3i — Environment potential adversaries

- Preserve the SRD `potential_adversaries_text` on imported environment flags.
- After adversary/environment compendiums are rebuilt, resolve environment groups against `dh-adversaries` UUIDs.
- Preserve generic source labels such as `Any`, `None`, or broad categories as structured text-only groups rather than inventing a specific adversary.
- Resolve exact names, common plurals, and known extraction/OCR variants deterministically.
- Remove `system.potentialAdversaries:uuid-resolution` only after the structured field is written.
- Semantic audit now flags an environment when source potential-adversary text exists but the structured Foundry field is empty.

Expected result from the previously GREEN P2.3.3h baseline: 701 -> 654 explicit mapping gaps, with `statIssues = 0`.
