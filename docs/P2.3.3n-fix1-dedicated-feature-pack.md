# P2.3.3n-fix1 — Dedicated Blood Hunter feature pack

## But

Corriger le rangement des 16 `Item: feature` du Blood Hunter v1.5 introduits par P2.3.3n.
Les features de support ne doivent pas gonfler les family packs `dh-classes` et `dh-subclasses`.

## Changement

- nouveau compendium `dh-features` (`Item`), déclaré dans `module.json` ;
- les 16 features Blood Hunter sont créées dans `dh-features` ;
- les liens natifs `system.features` des 1 classe + 3 sous-classes pointent vers ces UUID ;
- migration ciblée : suppression uniquement des anciens documents `supportManaged` créés par P2.3.3n dans `dh-classes` / `dh-subclasses` ;
- les documents canoniques `managed` ne sont pas touchés par cette migration ;
- audit supplémentaire : `dh-features = 16` et aucun `supportManaged` résiduel dans les packs classe/sous-classe.

## Cible

- `dh-classes`: 14 entrées
- `dh-subclasses`: 29 entrées
- `dh-features`: 16 entrées de support Blood Hunter
- top-level canonical documents audités: 1012
- mapping gaps: 334
- stat issues: 0
- green: true

Les 16 features restent du contenu playtest The Void / Blood Hunter v1.5 et aucune automatisation supplémentaire n'est inventée.
