# P2.10j.3f — Actions dans la colonne Objet

- Les actions sont placées dans la même ligne que `Rendre au personnage`.
- Chaque clic recharge le manifest frais puis retrouve l'entrée par
  `containerId + entryId`.
- Les événements click/pointerdown sont isolés du renderer parent.

Actions :
- Consommer
- Marquer modifié
- Supprimer
