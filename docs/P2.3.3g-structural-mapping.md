# P2.3.3g — Structural mapping

- Resolves subclass `linkedClass` after family-pack creation, using managed class UUIDs.
- Normalizes the six known SRD extraction tokens in adversary Experiences (`+/two.tnum`, `/comma.tab`, etc.).
- Treats absent Minion thresholds as canonical semantics rather than a mapping gap.
- Maps explicit `stress_none: true` to zero Stress and preserves a provenance flag.
- Reclassifies the four Spellcast Arcane-Frame Wheelchairs as an explicit dynamic-trait limitation, not a stat error.

Still intentionally unresolved: core class domains/evasion (not present in the current neutral class index), partial non-Minion thresholds, environment adversary UUIDs, and embedded feature automation.
