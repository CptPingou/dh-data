/**
 * P2.6.6c4 tombstone.
 *
 * Hunting Contextual Cards are now native Daggerheart `domainCard` Items.
 * Custom actor-sheet presentation from P2.6.6c1/c2 is intentionally retired.
 *
 * This inert export is kept only because changed-files-only ZIP patches cannot
 * remove an already-installed file reliably.
 */
export function registerContextualCardPresentation() {
  return Object.freeze([]);
}
