# P2.8j.1 — Opportunity writer hotfix

Fixes the P2.8j regression where `getOpportunityCountdown()` and
`getOpportunityValue()` were accidentally removed while replacing the old
DOM writer block.

No behavior change beyond restoring these public/read helpers.
