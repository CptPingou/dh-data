# P2.8n.1a — Libellé Chasse dans le Loadout Actor

Correctif ciblé du bridge P2.8n.1.

- conserve `system.domain = "valor"` pour la compatibilité Foundryborne ;
- conserve `toolkitCard.family = "hunt"` comme source de vérité ;
- ajoute le traitement de `renderActorSheet` ;
- repère chaque Item Toolkit embarqué par ID, avec fallback par nom ;
- remplace uniquement le badge de domaine situé dans la ligne de cette carte ;
- ne modifie aucune donnée du document.

## Test
Après F5, ouvrir l'onglet Loadout du personnage.
`Feinte d’approche`, `Guidage du Finisher` et les autres cartes Chasse doivent
afficher `Chasse` au lieu de `Valor`.

Le test console P2.8n.1 reste valide :
```js
game.modules.get("daggerheart-campaign-toolkit").api.toolkitCardPresentation.status()
```
