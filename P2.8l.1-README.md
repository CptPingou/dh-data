# P2.8l.1 — Consolidation Finisher + Support

Patch-only.

## Changements
- `Conversion`
  - restaure l'ActionField `MHFINACTION00001` (`attack`);
  - restaure `engagementActions.MHFINACTION00001.role = "finisher"`;
  - corrige `huntCardMechanic.hopeCost` de `0` vers `1`.
- `Appui défensif`
  - conserve `MHSUPACTION00001`;
  - retire de la source les champs `damage`, `roll` et `save` que Daggerheart 2.9.4 normalise hors d'un `DHEffectAction`, afin d'aligner source et runtime.

## Inchangé
- `engagement-finisher.mjs` : déjà conforme (coût 1 Hope, max 4 OP, clear OP, chat Conversion).
- `engagement-support.mjs` : déjà conforme (coût universel 1 Hope, effet possédé par la carte, application manuelle).
- `monster-hunter-engagement-contract.mjs` : déjà conforme (`finisherHopeCost: 1`).

## Vérification attendue après sync
- autonomous: 811/811 GREEN
- domain cards: 239/239 GREEN
- MH corpus: 8 cartes / 3 mécaniques GREEN
- actions natives:
  - Ouverture: 1
  - Conversion: 1
  - Appui défensif: 1
